#include "main.h"

/**
 * print_alphabet_x10 - this prints the alphabet ten times
 */

void print_alphabet_x10(void)
{
	int ten;
	char la;

	for (ten = 0; ten <= 9; ten++)
	{
		for (la = 'a'; la <= 'a'; la++)
			_putchar(la);
		_putchar('\n');
	}
}
